# Domain (sslip.io resolves to the embedded IP)
DOMAIN=studielan.77-42-36-80.sslip.io

# Basic auth for Caddy — generate hash:
#   docker run --rm caddy caddy hash-password --plaintext "ditt-passord"
AUTH_USER=admin
AUTH_HASH=$2a$14$...
